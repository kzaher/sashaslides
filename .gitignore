# Node
node_modules/

# Bazel outputs
bazel-*

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Generated protobuf code (non-Bazel)
proto/sashaslides_pb2.py
proto/sashaslides_pb2.pyi

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
*.env.local
venv/
.venv/

# Auth tokens / OAuth credentials (never commit)
.auth/*.json
.auth/tokens.json
.auth/google_oauth.json

# Data
data/
*.db

# Docker
.docker/
.claude/worktrees/*
.claude/scheduled_tasks.lock

# html2slides local ratings backups
analysis/renderer/html2slides/e2e/.ratings-complex.json
analysis/renderer/html2slides/e2e/.ratings-basics.json
.bug-solving-scratch/

# Google-table reverse-engineering fuzz outputs (regenerable; bulky PNGs + measured data)
renderer/html2slides/e2e/_fuzz/
renderer/html2slides/e2e/_gtl-fixtures/
renderer/html2slides/e2e/_border/
renderer/html2slides/e2e/_gtl-out/
renderer/html2slides/e2e/gtl-dataset*.json
renderer/html2slides/e2e/google-table-measurements.json

# html2slides distribution build output — regenerable via `npm run build`
# (mirrors source path: dist/renderer/html2slides/{browser,addon}/...).
# Note: the generic `dist/` above already matches this; kept explicit for clarity.
/dist/

# Local SxS ratings backups (survive /tmp wipes; user-specific, not committed)
renderer/html2slides/e2e/.ratings-backup/

.sxs-complex/
# bug_solving persistent rating ledger
.bug-solving-history/
