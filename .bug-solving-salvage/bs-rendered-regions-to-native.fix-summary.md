Root cause: the file-write conversion path in `runConvertPptx` (`renderer/html2slides/convert-pptx-io.ts`) uses STOCK pptxgenjs (`pres.writeFile`), where three OOXML fixes are NOT native — they only exist in the vendored fork (`deps/pptxgenjs/src`) consumed by the in-memory `buildPptxInMemory` path. A prior refactor deleted the zip post-patch calls here on the false assumption that all fixes had gone native (the stale comment at `convert-pptx-io.ts:404`), which regressed slide_12: the `.screen` shape's `round2SameRect` fell back to a bare `adj` handle (default oversized radius / wrong corners), and 43 no-stroke shapes emitted Google's default ~1pt strokes ("black borders too big"). The fix re-applies the missing post-patches after gradient/stroke injection at `convert-pptx-io.ts:384-386` — `injectRound2SameRectAdj` (adj1/adj2 handle naming), `injectShapeNoLine` (empty `<a:ln>` → `<a:noFill/>`), and `injectCellNoBorder` (table-cell no-border form) — using the file-based wrappers already defined at `convert-pptx-io.ts:254-283`, so the file-write path now matches the product / rated-good in-memory path; the misleading comment block at `convert-pptx-io.ts:407-415` was corrected to document that these are zip post-patches, not native, on this path.
