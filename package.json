{
  "scripts": {
    "sudo_claude": "claude --dangerously-skip-permissions",
    "sudo_claude_fork": "claude --dangerously-skip-permissions --fork-session --resume"
  },
  "dependencies": {
    "googleapis": "^171.4.0",
    "postcss-value-parser": "^4.2.0"
  }
}
