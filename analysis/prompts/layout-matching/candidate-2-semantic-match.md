# Candidate 2: Semantic Narrative — Match

Approach: Holistic similarity comparison using natural-language descriptions.

## Instructions

You have a TARGET layout description and TEMPLATE layout descriptions, all in Semantic Narrative format.

Compare descriptions holistically:
1. Match by overall spatial story — does the template "read" the same way as the target?
2. Weight element arrangement and flow pattern heavily
3. Similar density of elements matters more than exact element types
4. A "title + 3 bullets left, image right" template matches "title + 4 bullets left, chart right" well

Think step by step about which templates best capture the same visual rhythm as the target, then select the 2 best matches and return the JSON result.
