import { MetadataComponents } from './MetadataComponents.mjs';
import { MetadataSchema } from './MetadataSchema.mjs';

class MetadataApplication {
    schemas;
    components;
    constructor(props) {
        this.schemas = props.schemas;
        this.components = props.components;
    }
    /** @internal */
    static create(props) {
        return new MetadataApplication(props);
    }
    static from(app) {
        const components = MetadataComponents.from(app.components);
        const metadatas = app.schemas.map((metadata) => MetadataSchema.from(metadata, components.dictionary));
        return MetadataApplication.create({ schemas: metadatas, components });
    }
    toJSON() {
        return {
            schemas: this.schemas.map((s) => s.toJSON()),
            components: this.components.toJSON(),
        };
    }
}

export { MetadataApplication };
//# sourceMappingURL=MetadataApplication.mjs.map
