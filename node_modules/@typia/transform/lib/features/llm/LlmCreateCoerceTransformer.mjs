import { LlmMetadataFactory, LlmCoerceProgrammer, MetadataFactory, MetadataCollection } from '@typia/core';
import ts from 'typescript';
import { TransformerError } from '../../TransformerError.mjs';

var LlmCreateCoerceTransformer;
(function (LlmCreateCoerceTransformer) {
    LlmCreateCoerceTransformer.transform = (props) => {
        // GET GENERIC ARGUMENT
        if (!props.expression.typeArguments?.length)
            throw new TransformerError({
                code: "typia.llm.createCoerce",
                message: "no generic argument.",
            });
        const top = props.expression.typeArguments[0];
        if (ts.isTypeNode(top) === false)
            return props.expression;
        // GET TYPE AND CONFIG
        const config = LlmMetadataFactory.getConfig({
            context: props.context,
            method: "createCoerce",
            node: props.expression.typeArguments[1],
        });
        const type = props.context.checker.getTypeFromTypeNode(top);
        if (type.isTypeParameter())
            throw new TransformerError({
                code: "typia.llm.createCoerce",
                message: "non-specified generic argument.",
            });
        // VALIDATE TYPE
        const analyze = (validate) => {
            const result = MetadataFactory.analyze({
                checker: props.context.checker,
                transformer: props.context.transformer,
                options: {
                    absorb: validate,
                    escape: true,
                    constant: true,
                    validate: validate === true
                        ? (next) => LlmCoerceProgrammer.validate({
                            config,
                            metadata: next.metadata,
                            explore: next.explore,
                        })
                        : undefined,
                },
                components: new MetadataCollection({
                    replace: MetadataCollection.replace,
                }),
                type,
            });
            if (result.success === false)
                throw TransformerError.from({
                    code: "typia.llm.createCoerce",
                    errors: result.errors,
                });
            return result.data;
        };
        analyze(true);
        // GENERATE CODE (factory returns the function directly)
        return LlmCoerceProgrammer.write({
            context: props.context,
            modulo: props.modulo,
            metadata: analyze(false),
            name: top.getFullText().trim(),
            config,
        });
    };
})(LlmCreateCoerceTransformer || (LlmCreateCoerceTransformer = {}));

export { LlmCreateCoerceTransformer };
//# sourceMappingURL=LlmCreateCoerceTransformer.mjs.map
